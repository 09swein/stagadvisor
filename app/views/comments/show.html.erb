/*This does not show up on the bottom of the article. You would be looking for _comment.html.erb partical 
*/
<p id="notice"><%= notice %></p>

<p>
  <strong>Place:</strong>
  <%= @comment.place_id %>
</p>

<p>
  <strong>Body:</strong>
  <%= @comment.body %>
</p>
<%if current_user.try(:admin?) %>
	<%= link_to 'Edit', edit_comment_path(@comment) %> |
	<%= link_to 'Back', comments_path %>
<%end%>